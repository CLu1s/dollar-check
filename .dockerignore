# Se reinstala/regenera dentro de la imagen
node_modules
dist

# Estado y secretos: nunca en la imagen (en CIAB llegan por BOTTLE_* y la app Secrets)
data
backups
logs
.env
.env.example
*.db
*.db-wal
*.db-shm

# Metadata local
.git
.gitignore
.idea
.claude
.DS_Store

# Infra y docs: no hacen falta en runtime.
# El Dockerfile no se lista: buildah lo lee del contexto.
cloudinabottle.toml
.dockerignore
scripts
*.md

# Deploy anterior en macOS/launchd
com.dollarcheck.bot.plist
